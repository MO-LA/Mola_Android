<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.molaschoolproject">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MolaSchoolProject"
        android:usesCleartextTraffic="true">
        <activity android:name=".activity.CommunityDetailActivity"></activity>
        <activity android:name=".activity.MyPageActivity" />
        <activity android:name=".activity.WordSearchActivity" />
        <activity android:name=".activity.SchoolDetailActivity" />
        <activity android:name=".activity.MainActivity">

        </activity>
        <activity android:name=".activity.LocalSearchActivity" />
        <activity android:name=".activity.SchoolSearchActivity" />
        <activity android:name=".activity.SignUpActivity" />
        <activity android:name=".activity.LoginActivity" >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.CommunityActivity" />
        <activity android:name=".activity.CommunityWritingActivity">

        </activity>
    </application>

</manifest>